{
  "name": "pnppowershell",
  "version": "0.0.1",
  "description": "PnP-Powershell VSTS task",
  "main": "pnp-powershell.js",
  "scripts": {
    "build": "tsc -p .",
    "package": "npm run build && cd .. && tfx extension create --manifest-globs vss-extension.json",
    "publish": "npm run package && cd .. && tfx extension publish --share-with mvapps-vsts --rev-version"
  },
  "author": "Sergei Sergeev",
  "license": "MIT",
  "dependencies": {
    "uuid": "^3.2.1",
    "vsts-task-lib": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^10.3.1",
    "tslint": "^5.10.0",
    "typescript": "^2.9.1"
  }
}
